{
    "type": "object",
    "properties": {
        "spider_type": {
            "type": "string",
            "default": "zzh"
        },
        "job_id": {
            "type": "string"
        },

        "maxdepth": {
            "type": "integer",
            "minimum": 0,
            "maximum": 3,
            "default": 0
        },
        "expires": {
            "type": "integer",
            "default": 0
        },

        "url": {
            "type": "string"
        },

        "callback": {
            "type": "string",
            "default": "parse_inform_index"
        },

        "errback": {
            "type": "string",
            "default": "parse_inform_index"
        },

        "method": {
            "type": "string"
        },

        "headers": {
            "type": "object"
        },

        "body": {
            "type": "string"
        },

        "cookies": {
            "type": "object"

        },

        "_encoding": {
            "type": "string"

        },

        "priority": {
            "type": "integer",
            "minimum": 0,
            "maximum": 1000,
            "default": 1
        },

        "dont_filter": {
            "type": "boolean"

        },

        "meta": {
            "type": "object",
            "default": null
        },
        "_class": {
            "type": "string"
        }
    },
    "required": [
        "url",
        "meta",
        "spider_type"
    ],
    "additionalProperties": false
}